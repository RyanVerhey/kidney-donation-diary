version: 1
frontend:
  phases:
    preBuild:
      commands:
        - bundle install --deployment
    build:
      commands:
        - bundle exec middleman build
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths:
      - vendor/**/*
